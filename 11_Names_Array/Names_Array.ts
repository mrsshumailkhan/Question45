// Storing names in an array called names
let names: string[] = ["Khizer", "Azeen", "Khalil", "Shumail"];

// Printing each person's name by accessing each element in the list one at a time
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}

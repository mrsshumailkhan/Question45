// Define an array to store examples of favorite modes of transportation

let transportationModes: string[] = ["motorcycle", "car", "bicycle", "scooter"];

// Iterate over the array and print statements about each item

for (let i = 0; i < transportationModes.length; i++) {
    console.log(`I would like to buy a ${transportationModes[i]}.`);
}
